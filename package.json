{
  "name": "nextjs",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build && next export && next-sitemap --config next-sitemap.js",
    "start": "next start",
    "lint": "next lint",
    "export": "next export",
    "deploy": "next-sitemap --config next-sitemap.js && git add . && git commit -m 'release' && git push origin master"
  },
  "dependencies": {
    "@notionhq/client": "^2.2.3",
    "bootstrap-icons": "^1.10.5",
    "next": "13.1.6",
    "next-i18next": "^14.0.0",
    "next-sitemap": "^4.0.9",
    "react": "18.2.0",
    "react-dom": "18.2.0",
    "react-twitter-embed": "^4.0.4"
  },
  "devDependencies": {
    "serve": "^14.2.0"
  }
}
